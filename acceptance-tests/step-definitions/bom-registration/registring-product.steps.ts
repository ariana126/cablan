import { Given, When, Then } from '@cucumber/cucumber';
import { Actor } from '@serenity-js/core';

When('{actor} محصول جدیدی با اطلاعات معتبر ثبت می کند', (_actor: Actor) => {
  return 'pending';
});

Then('محصول جدیدی در سیستم ثبت شده باشد', () => {
  return 'pending';
});

Given('اینکه یک محصول در سیستم ثبت شده باشد', () => {
  return 'pending';
});

When('{actor} اطلاعات آن محصول را ویرایش می کند', (_actor: Actor) => {
  return 'pending';
});

Then('اطلاعات ویرایش شده محصول در سیستم ثبت شده باشد', () => {
  return 'pending';
});

When('{actor} آن محصول را حذف می کند', (_actor: Actor) => {
  return 'pending';
});

Then('آن محصول از سیستم حذف شده باشد', () => {
  return 'pending';
});

When('{actor} تلاش می کند محصول جدیدی ثبت کند', (_actor: Actor) => {
  return 'pending';
});

Then('محصول جدیدی ثبت نشده باشد', () => {
  return 'pending';
});

When('{actor} تلاش می کند اطلاعات آن محصول را ویرایش کند', (_actor: Actor) => {
  return 'pending';
});

Then('محصول ویرایش نشده باشد', () => {
  return 'pending';
});

When('{actor} تلاش می کند آن محصول را حذف کند', (_actor: Actor) => {
  return 'pending';
});

Then('آن محصول از سیستم حذف نشده باشد', () => {
  return 'pending';
});

When('{actor} اطلاعات محصول جدید را وارد می کند', (_actor: Actor) => {
  return 'pending';
});

When('هیچ جزئی برای محصول ثبت نمی کند', () => {
  return 'pending';
});

Then('پیغام خطای حداقل یک جز برای محصول نشان داده شود', () => {
  return 'pending';
});

Given('اینکه یک محصول با حداقل یک جز در سیستم ثبت شده باشد', () => {
  return 'pending';
});

When('{actor} تمام اجزای آن محصول را حذف می کند', (_actor: Actor) => {
  return 'pending';
});

When('{actor} جز جدیدی برای آن محصول ثبت می کند', (_actor: Actor) => {
  return 'pending';
});

When('هیچ مواد اولیه ای برای جز ثبت نمی کند', () => {
  return 'pending';
});

Then('پیغام خطای حداقل یک مواد اولیه برای جز نشان داده شود', () => {
  return 'pending';
});

Given('اینکه یک جز با حداقل یک مواد اولیه در سیستم ثبت شده باشد', () => {
  return 'pending';
});

When('{actor} تمام مواد اولیه آن جز را حذف می کند', (_actor: Actor) => {
  return 'pending';
});

When('{actor} چند جز برای آن محصول ثبت می کند', (_actor: Actor) => {
  return 'pending';
});

Then('تمام اجزای ثبت شده به محصول مربوط باشند', () => {
  return 'pending';
});

Given('اینکه یک جز برای یک محصول در سیستم ثبت شده باشد', () => {
  return 'pending';
});
